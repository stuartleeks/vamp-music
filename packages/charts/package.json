{
  "name": "vamp-music-charts",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t magneticio/vamp-music-charts .",
    "docker:run": "docker run -d -P --name vamp-music-charts magneticio/vamp-music-charts",
    "docker:stop": "docker stop vamp-music-charts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^1.26.1",
    "good": "^7.2.0",
    "good-console": "^6.4.0",
    "hapi": "^16.5.0",
    "mongoose": "^4.11.4"
  },
  "devDependencies": {
    "lerna": "^2.0.0"
  }
}
